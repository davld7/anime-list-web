# Configuration for the anime-list-web frontend.
#
# API_BASE_URL is read from the process environment at runtime (app/config.py).
# A project-root .env file is loaded automatically; a value already set in the
# environment takes precedence. Example:
#   API_BASE_URL=http://localhost:8000
API_BASE_URL=http://localhost:8000